// TODO: Rename these to specify that they apply to proxy/caching.
// TODO: Add a variable for the before and after directory.
export const routeBase: string = '/~/';
export const cacheDirectoryPath: string = '.cache';
export const maxAgeMilliseconds: number = 7 * 24 * 60 * 60 * 1000;
export const beforeAndAfterDirectoryPath: string = '.before-and-after';
export const preserveContentEncoding: boolean = true;
